# Port nginx expose ra ngoài
HTTP_PORT=8080

# MariaDB
DB_ROOT_PASSWORD=change_this_root_password
DB_DATABASE=sspanel
DB_USERNAME=sspanel
DB_PASSWORD=change_this_db_password

# Redis (để trống nếu không cần password)
REDIS_PASSWORD=
